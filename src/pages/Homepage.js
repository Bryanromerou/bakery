import React from 'react';
import Carousel from '../components/home/Carousel';

const Homepage = () => {
    return (
        <div>
            <Carousel/>
        </div>
    );
}

export default Homepage;
